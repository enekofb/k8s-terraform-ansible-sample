---

- name: Create kubernetes bootstrap token
  command: bash -c "head -c 16 /dev/urandom | od -An -t x | tr -d ' '"
  register: token_result
  run_once: true

- set_fact:
    bootstrap_token: "{{ token_result.stdout }}"
  run_once: true

- debug: var=bootstrap_token
